

function Home(props: { backURL: string }) {

    // バックエンド 元URL
    const url = props.backURL;

    console.log(url)

    return (
        <div>
            aaaaa
        </div>
    )
}

export default Home